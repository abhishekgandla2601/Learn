# name = input("Enter your full name: ")
phone_number = input("Enter your phone number: ")
# result = len(name)# it returns int, it also counts
#
# result = name.find("A")# finds the index
# result = name.rfind("q")# finds the last index where the letter or the character got occured. gives negative value if not found.
# result = name.capitalize()# makes the first letter capital, it means the 0-index
# result = name.upper()# makes the all characters upper case
# result = name.lower()# makes all the characters lower case
# result = name.isdigit() # gives is int or not
# result = name.isalpha() # finds is an alphabet or not
# result = phone_number.count('-')  # counts the character
#
# print(result)