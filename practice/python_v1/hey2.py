# id() It is a predefine function in python. 
# The main objective os to know the address of a variable at memory allocation where out data is stored
pid = 1001

print(pid)

print("The address of pid is: ", id(pid))# the address of pid variable will change every time you run the program