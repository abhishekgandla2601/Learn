class car:
    def __init__(self,model, year, color, for_sale):# self means the object we are creating right now
        self.model = model
        self.year = year
        self.color = color
        self.for_sale = for_sale
